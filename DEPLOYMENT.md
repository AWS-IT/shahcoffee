# 🚀 Инструкция по развертыванию на сервер с nginx

## 📋 Требования

- Node.js 18+ (или использовать nvm)
- npm или yarn
- nginx
- SSL сертификат (Let's Encrypt)
- Git (для pull updates)
- PM2 (опционально, для управления Node.js процессом)

## 🔧 Установка на сервере

### 1. Подготовка директорий

```bash
# Создаем директорию для проекта
sudo mkdir -p /var/www/shahcoffee
sudo chown $USER:$USER /var/www/shahcoffee

# Переходим в директорию
cd /var/www/shahcoffee

# Клонируем репозиторий
git clone <ваш_репозиторий> .
```

### 2. Установка зависимостей

```bash
# Установка npm пакетов (включая dev для сборки)
npm install

# Установка PM2 глобально (для управления Node.js)
npm install -g pm2
```

### 3. Сборка React проекта

```bash
npm run build
```

Это создаст папку `dist/` с готовым фронтендом.

### 4. Настройка .env на сервере

```bash
# Создаем .env файл
nano .env
```

Добавьте переменные окружения:

```env
MOYSKLAD_PUBLIC_TOKEN=ваш_токен
VITE_MOYSKLAD_TOKEN=ваш_токен
ROBOKASSA_MERCHANT_ID=CofeeShah
ROBOKASSA_PASS1=ваш_пароль1
ROBOKASSA_PASS2=ваш_пароль2
```

### 5. Запуск Node.js сервера с PM2

```bash
# Запускаем сервер через PM2
pm2 start server.js --name "shahcoffee-api"

# Сохраняем конфигурацию PM2 (автозапуск при перезагрузке)
pm2 save
pm2 startup

# Проверяем статус
pm2 status
pm2 logs shahcoffee-api
```

### 6. Настройка nginx

```bash
# Копируем конфиг
sudo cp nginx.conf /etc/nginx/sites-available/shahcoffee

# Создаем символическую ссылку
sudo ln -s /etc/nginx/sites-available/shahcoffee /etc/nginx/sites-enabled/

# Проверяем конфиг
sudo nginx -t

# Перезагружаем nginx
sudo systemctl reload nginx
```

### 7. Получение SSL сертификата (Let's Encrypt)

```bash
# Установка certbot
sudo apt-get install certbot python3-certbot-nginx

# Получение сертификата
sudo certbot certonly --nginx -d shahshop.ru -d www.shahshop.ru

# Автоматическое обновление сертификата
sudo systemctl enable certbot.timer
```

## 🔄 Обновление проекта

```bash
# Перейди в директорию проекта
cd /var/www/shahcoffee

# Обновляем код
git pull origin main

# Переустанавливаем зависимости (если были изменения)
npm ci --omit=dev

# Пересобираем фронтенд
npm run build

# Перезапускаем API сервер
pm2 restart shahcoffee-api

# Перезагружаем nginx (если нужно)
sudo systemctl reload nginx
```

**Или используй готовый скрипт:**

```bash
chmod +x deploy.sh
./deploy.sh
```

## 📝 Структура на сервере

```
/var/www/shahcoffee/
├── dist/                 # Сборка React (раздается через nginx)
├── src/                  # Исходный код React
├── server.js             # Node.js API сервер (порт 3001)
├── .env                  # Переменные окружения
├── nginx.conf            # Конфиг nginx
└── package.json
```

## 🌐 Как это работает

1. **nginx** слушает порты 80 и 443
2. Запросы на `/` → раздаются файлы из `dist/` (React SPA)
3. Запросы на `/api/` → проксируются на `localhost:3001` (Node.js)
4. Robokassa callback идет на `/api/robokassa/result` → `localhost:3001/robokassa/result`

## 🔒 Безопасность

- SSL/TLS включены (обязательно)
- HTTP редирект на HTTPS
- Node.js процесс управляется PM2 (автоперезапуск при крахе)
- .env файл НЕ коммитится в git (добавить в .gitignore)

## 📊 Мониторинг

```bash
# Смотреть логи PM2
pm2 logs shahcoffee-api

# Смотреть логи nginx
tail -f /var/log/nginx/shahcoffee-error.log
tail -f /var/log/nginx/shahcoffee-access.log

# CPU/Memory использование
pm2 monit
```

## 🆘 Troubleshooting

**Ошибка 403 на Robokassa callback:**
- Проверь пароли в `.env` файле
- Убедись что URL в Robokassa настроен на `https://shahshop.ru/api/robokassa/result`

**Node.js процесс падает:**
- Проверь логи: `pm2 logs shahcoffee-api`
- Убедись что .env содержит все нужные переменные
- Проверь права доступа на файлы

**nginx возвращает 502:**
- Проверь что Node.js сервер запущен: `pm2 status`
- Убедись что порт 3001 открыт: `netstat -tlnp | grep 3001`

## 📞 Поддержка

Если что-то не работает:
1. Проверь логи (PM2 и nginx)
2. Убедись что порты открыты
3. Проверь переменные окружения в .env
4. Убедись что сертификаты SSL правильно установлены
