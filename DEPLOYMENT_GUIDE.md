# Инструкция по настройке Nginx для shahcoffee

## Проблемы, которые решаются:
1. ✅ Доступ к сайту без указания порта (через http://37.140.192.62 вместо http://37.140.192.62:3001)
2. ✅ Работа роутинга React (в том числе /admin)

## Что было исправлено в коде:

### 1. server.js
Убрано условие `req.path.startsWith('/admin')` из SPA fallback, чтобы React Router мог обрабатывать все клиентские маршруты.

## Инструкция по настройке на сервере:

### Шаг 1: Подключитесь к серверу через SSH
```bash
ssh root@37.140.192.62
# или
ssh ваш_пользователь@37.140.192.62
```

### Шаг 2: Установите Nginx (если не установлен)
```bash
# Для Ubuntu/Debian
sudo apt update
sudo apt install nginx -y

# Для CentOS/RHEL
sudo yum install nginx -y
```

### Шаг 3: Скопируйте конфигурационный файл на сервер
На вашем локальном компьютере (из папки проекта):
```bash
# Скопируйте файл nginx.conf на сервер
scp nginx.conf root@37.140.192.62:/tmp/shahcoffee.conf
```

### Шаг 4: Установите конфигурацию Nginx на сервере
```bash
# На сервере выполните:
sudo cp /tmp/shahcoffee.conf /etc/nginx/sites-available/shahcoffee
sudo ln -s /etc/nginx/sites-available/shahcoffee /etc/nginx/sites-enabled/shahcoffee

# Удалите дефолтную конфигурацию (опционально)
sudo rm /etc/nginx/sites-enabled/default
```

### Шаг 5: Проверьте конфигурацию Nginx
```bash
sudo nginx -t
```
Должно появиться сообщение: `syntax is ok` и `test is successful`

### Шаг 6: Перезапустите Nginx
```bash
sudo systemctl restart nginx
sudo systemctl enable nginx  # Автозапуск при старте системы
```

### Шаг 7: Убедитесь, что ваше Node.js приложение запущено
```bash
# Проверьте, работает ли приложение на порту 3001
sudo netstat -tlnp | grep 3001
# или
sudo ss -tlnp | grep 3001
```

Если приложение не запущено, запустите его:
```bash
cd /путь/к/вашему/проекту
npm run build  # Собрать проект
npm start      # Запустить сервер
```

### Шаг 8: Настройте автозапуск приложения через PM2 (рекомендуется)
```bash
# Установите PM2
sudo npm install -g pm2

# Запустите приложение
cd /путь/к/вашему/проекту
pm2 start npm --name "shahcoffee" -- start

# Сохраните конфигурацию PM2
pm2 save
pm2 startup
```

### Шаг 9: Откройте порт 80 в файерволе (если используется)
```bash
# Для UFW (Ubuntu)
sudo ufw allow 80/tcp
sudo ufw allow 'Nginx Full'

# Для firewalld (CentOS/RHEL)
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --reload
```

## Проверка работы:

1. **Главная страница:** http://37.140.192.62
2. **Админка:** http://37.140.192.62/admin
3. **Каталог:** http://37.140.192.62/catalog
4. **О нас:** http://37.140.192.62/about

## Если что-то не работает:

### Проверьте логи Nginx:
```bash
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/shahcoffee_error.log
```

### Проверьте логи приложения:
```bash
# Если используете PM2
pm2 logs shahcoffee

# Если запускали вручную
# Проверьте консоль, где запущен npm start
```

### Проверьте статус Nginx:
```bash
sudo systemctl status nginx
```

### Проверьте статус приложения:
```bash
# С PM2
pm2 status

# Без PM2
ps aux | grep node
```

## Важные моменты:

1. **Домен:** Если у вас есть доменное имя, замените `37.140.192.62` на ваш домен в файле `/etc/nginx/sites-available/shahcoffee` в строке `server_name`

2. **HTTPS:** Для использования HTTPS установите SSL сертификат:
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d ваш_домен.ru
```

3. **Перезапуск после изменений:** После любых изменений в nginx.conf:
```bash
sudo nginx -t && sudo systemctl reload nginx
```

4. **Обновление кода:** После загрузки нового кода на сервер:
```bash
cd /путь/к/проекту
git pull  # если используете git
npm run build
pm2 restart shahcoffee
```

## Структура проекта на сервере должна быть примерно такой:
```
/home/ваш_пользователь/shahcoffee/
├── dist/              # Собранный фронтенд (после npm run build)
├── src/               # Исходники
├── server.js          # Node.js сервер
├── package.json
├── .env               # Переменные окружения
└── ...
```

## Готово! 🎉
Теперь ваш сайт должен быть доступен по адресу http://37.140.192.62 без указания порта, и админка должна работать по адресу http://37.140.192.62/admin
